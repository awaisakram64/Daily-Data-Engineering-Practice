# Let's say we have two lists: names and ages
names = ['Alice', 'Bob', 'Charlie']
ages = [25, 30, 35]

# We can use the zip function to combine them into a list of tuples
combined = list(zip(names, ages))

print(combined)